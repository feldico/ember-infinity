<ul
  class="demo-items list-items"
  ...attributes
>
  <button type="button" class="filter-posts" {{on 'click' (fn @filterPosts @model)}}>Filter Posts</button>
  <button type="button" class="flush-posts" {{on 'click' (fn @flushPosts @model)}}>Flush Posts</button>
  {{#each @model as |item|}}
    <li>{{item.id}} · {{item.name}}</li>
  {{/each}}

  <InfinityLoader @infinityModel={{@model}} />
</ul>